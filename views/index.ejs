<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Weather APP</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.1.0/css/all.css">
</head>
<body>
    <div class="container mt-5">
        <div class="row">
            <div class="col-6 text-center mx-auto">
                <h3>Weather APP</h3>
                <form action="/weather" method="post" class="my-4">
                    <div class="form-group d-flex">
                        <input type="text" class="form-control mx-3" name="city" placeholder="Enter city name" autocomplete="off" autofocus>
                        <button type="submit" class="btn btn-secondary">Get Weather</button>
                    </div>
                </form>

                <% if (locals.data) { %>
                <div class="card mx-3">
                    <div class="card-body">
                        <p class="card-text"><i class="fas fa-thermometer-half"></i> <%= (data.main.temp - 273.15).toFixed(2) %>&deg;C</p>
                        <p class="card-text"><i class="fas fa-cloud"></i> <%= data.weather[0].main %></p>
                        <p class="card-text"><i class="fas fa-map-marker-alt"></i> <%= data.name %>, <%= data.sys.country %></p>
                    </div>
                </div>
                <% } %>

                <% if (locals.error) { %>
                <div class="error" role="alert">
                    <%= locals.error %>
                </div>
                <% } %>
            </div>

        </div>
    </div>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous"></script>
</body>
</html>